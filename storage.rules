rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow authenticated users to upload, read, and delete outfit images
    match /outfits/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}

